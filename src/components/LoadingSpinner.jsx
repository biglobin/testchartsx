import React from 'react';

const LoadingSpinner = () => {
  return (
    <div className="loading-spinner" style={{ borderLeftColor: 'var(--primary-color)' }}></div>
  );
};

export default LoadingSpinner;